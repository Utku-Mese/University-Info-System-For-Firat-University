//TODO: set your db settings

const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: '%HQznV8*',
        database: 'myFirstTrial'
    }
}

module.exports = config;