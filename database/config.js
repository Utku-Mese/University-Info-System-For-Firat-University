//TODO: set your db settings

const config = {
    db: {
        host: 'localhost',
        user: 'your db user',
        password: 'your db password*',
        database: 'your db name'
    }
}

module.exports = config;
